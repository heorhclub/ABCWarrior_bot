# ────────────────────────────────────────────────────────────────
# Example .env file for ABCWarrior_bot (English version)
# Do NOT commit your real .env file with actual values!
# Copy this file to .env and fill in your own values
# ────────────────────────────────────────────────────────────────

# Bot token from @BotFather (required)
BOT_TOKEN=

# Your Telegram ID (Owner) — only this person can use owner-only commands
# (/mute15, /mute60, /mute24h, /mute666, /unmute, /listmute, /test without deletion)
# How to find your ID: send a message to @userinfobot
OWNER_ID=

# Optional: Separate ID for sending private messages to the owner
# Useful when posting anonymously from a channel (discussion group)
# If left empty or equal to OWNER_ID — falls back to OWNER_ID
OWNER_PRIVATE_ID=

# Comma-separated list of allowed chat IDs (groups/supergroups) without spaces
# Example: -1001234567890,-1009876543210
# Leave empty to allow the bot in all groups (not recommended for security)
ALLOWED_CHAT_IDS=

# ─── Anti-flood limits (adjust as needed) ───
# Maximum messages per day (UTC) from one user
DAILY_MESSAGE_LIMIT=200

# Maximum messages in the last hour
HOURLY_MESSAGE_LIMIT=100

# Mute duration (minutes) for exceeding hourly limit
HOURLY_MUTE_MINUTES=15

# Short-term anti-flood: max messages in the last N minutes
SHORT_TERM_MESSAGE_LIMIT=10
SHORT_TERM_WINDOW_MINUTES=5

# Mute duration (minutes) for exceeding short-term limit
SHORT_TERM_MUTE_MINUTES=3

# Mute duration (minutes) for sending voice messages (non-admins/creators only)
VOICE_MUTE_MINUTES=30

# Mute duration (days) for exceeding daily limit
DAILY_MUTE_DAYS=7

# ─── Exemption from anti-flood counters (true/false) ───
# Privileged users bypass text message flood counting
# (voice message restriction still applies unless they are admin/creator)

# Bot owner (OWNER_ID) is exempt from anti-flood limits
EXEMPT_OWNER_ANTIFLOOD=true

# Group creator is exempt from anti-flood limits
EXEMPT_CREATOR_ANTIFLOOD=true

# Group administrators are exempt from anti-flood limits
EXEMPT_ADMIN_ANTIFLOOD=true

# ────────────────────────────────────────────────────────────────
# Optional / future variables
# LOGGER_LEVEL=INFO  # Possible values: DEBUG, INFO, WARNING, ERROR
# ────────────────────────────────────────────────────────────────
